# @banzae/agent-runtime-core/experimental public API

> Generated by `pnpm api:extract`. Review and commit intentional changes.

- **assertStartRunInput** — experimental — `FunctionDeclaration`
  - `(input: { idempotencyKey: string; applicationRunId: string; }) => void`
- **canonicalJson** — experimental — `FunctionDeclaration`
  - `(value: unknown) => string`
- **collectBytes** — experimental — `FunctionDeclaration`
  - `(body: AsyncIterable<Uint8Array>, maxBytes?: number) => Promise<Uint8Array>`
- **connectionFingerprint** — experimental — `FunctionDeclaration`
  - `(crypto: RuntimeCrypto, input: Readonly<Record<string, unknown>>) => Promise<string>`
- **emptyAsyncIterable** — experimental — `FunctionDeclaration`
  - `<T>() => AsyncIterable<T>`
- **failedEventFromError** — experimental — `FunctionDeclaration`
  - `(base: RuntimeEventBase, error: RuntimeError) => RuntimeEvent`
- **mergeCapabilities** — experimental — `FunctionDeclaration`
  - `(base: RuntimeCapabilities, patch: Partial<RuntimeCapabilities>) => RuntimeCapabilities`
- **normalizeEndpoint** — experimental — `FunctionDeclaration`
  - `(endpoint: string) => string`
- **readJsonBody** — experimental — `FunctionDeclaration`
  - `(body: AsyncIterable<Uint8Array>, maxBytes?: number) => Promise<unknown>`
- **runtimeEventBase** — experimental — `FunctionDeclaration`
  - `(input: { ids: { id(): string; }; now: Date; type: RuntimeEventName; applicationRunId: string; externalRunId: string; externalSessionId: string; sequence?: number; provider?: RuntimeEventBase["provider"]; }) => RuntimeEventBase`
- **runWithConcurrencyLimit** — experimental — `FunctionDeclaration`
  - `<T, R>(items: readonly T[], limit: number, work: (item: T) => Promise<R>) => Promise<R[]>`
- **SequenceTracker** — experimental — `ClassDeclaration`
  - `typeof SequenceTracker`
- **validateInputCapabilities** — experimental — `FunctionDeclaration`
  - `(capabilities: RuntimeCapabilities, input: RuntimeUserInput) => void`
- **withDeadline** — experimental — `FunctionDeclaration`
  - `<T>(work: Promise<T>, timeoutMs: number, signal?: AbortSignal) => Promise<T>`
