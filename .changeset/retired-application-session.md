---
"@banzae/agent-runtime-openclaw": patch
---

Classify retired OpenClaw application sessions as a safe, structured conflict so consumers can rotate the session without retaining provider session keys.
