# Codex adapter placeholder

Status: not implemented and not published.

This directory reserves the future adapter boundary. It must not be registered
in runtime detection or used in production. Implementation must pass the shared
adapter conformance suite before publication.

Future checklist:

- probe/connect;
- health/capabilities;
- ensure session;
- start/stream/get/cancel run;
- history;
- approval support if available;
- sanitized errors and fixtures.
